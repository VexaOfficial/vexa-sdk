# @vexa-sdk/cli

CLI for Vexa SDK.

## Installation

```bash
pnpm add @vexa-sdk/cli
```

## Commands

- `vexa watch --pool <address> --threshold <n>`: Watch liquidity changes
- `vexa run-rule <rule.json> [--dry-run]`: Run automation rule